// TRACK STUB — owned by the Claim lane. The assigned agent replaces this
// body with the real screen. Keep the export names (barrel depends on them).
export interface ClaimValues { /* filled by track */ }
export interface ClaimProps { /* filled by track */ }
export function Claim(_props: ClaimProps) { return null; }
