// TRACK STUB — owned by the ForgotPassword lane. The assigned agent replaces this
// body with the real screen. Keep the export names (barrel depends on them).
export interface ForgotPasswordProps { /* filled by track */ }
export function ForgotPassword(_props: ForgotPasswordProps) { return null; }
