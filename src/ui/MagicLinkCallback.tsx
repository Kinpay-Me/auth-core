// TRACK STUB — owned by the MagicLinkCallback lane. The assigned agent replaces this
// body with the real screen. Keep the export names (barrel depends on them).
export interface MagicLinkCallbackProps { /* filled by track */ }
export function MagicLinkCallback(_props: MagicLinkCallbackProps) { return null; }
