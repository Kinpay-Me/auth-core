// TRACK STUB — owned by the MagicLinkRequest lane. The assigned agent replaces this
// body with the real screen. Keep the export names (barrel depends on them).
export interface MagicLinkRequestProps { /* filled by track */ }
export function MagicLinkRequest(_props: MagicLinkRequestProps) { return null; }
